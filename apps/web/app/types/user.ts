export type User = {
    id: string
    accountId: string
    firstName: string
    lastName: string
    email: string
    isActive: boolean
    role: string
    createdAt: string
    updatedAt: string
    deletedAt: string | null
}